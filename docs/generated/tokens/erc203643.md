## ERC203643InternalCommon

Aggregates the internal functions of ERC20, ERC3643

_Provides unified token transfer logic for both ERC20 and ERC3643 standards_

### \_beforeTokenTransfer

```solidity
function _beforeTokenTransfer(address _from, address _to, uint256 _amount) internal virtual
```

\_Hook that is called before any transfer of tokens. This includes
minting and burning.

Calling conditions:

- when `from` and `to` are both non-zero, `amount` of `from`'s tokens
  will be transferred to `to`.
- when `from` is zero, `amount` tokens will be minted for `to`.
- when `to` is zero, `amount` of `from`'s tokens will be burned.
- `from` and `to` are never both zero.

To learn more about hooks, head to xref:ROOT:extending-contracts.adoc#using-hooks[Using Hooks].\_

### \_handleMintOperation

```solidity
function _handleMintOperation(address _to) internal
```

### \_handleBurnOperation

```solidity
function _handleBurnOperation(address _from, uint256 _amount) internal
```

### \_handleTransferOperation

```solidity
function _handleTransferOperation(address _from, address _to, uint256 _amount) internal
```

### \_unfreezeIf3643Mode

```solidity
function _unfreezeIf3643Mode(address _from, uint256 _amount) internal
```

### \_hasIdentityRegistry

```solidity
function _hasIdentityRegistry() internal view returns (bool)
```

_Checks if identity registry is configured_

#### Return Values

| Name | Type | Description                      |
| ---- | ---- | -------------------------------- |
| [0]  | bool | True if identity registry exists |

### \_calculateFreeBalance

```solidity
function _calculateFreeBalance(address _account) internal view returns (uint256)
```

### \_isRecipientVerified

```solidity
function _isRecipientVerified(address _to) internal view
```
