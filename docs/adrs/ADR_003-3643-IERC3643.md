# ADR_003: Modularization of IERC3643 Interface in 3643 Architecture

# Table of contents

- [Status](#status)
- [Context](#context)
- [Decision](#decision)
    - [1. Segregation of IERC3643 into functional modules](#1-segregation-of-IERC3643-into-functional-modules)
    - [2. Reuse of existing modules](#2-reuse-of-existing-modules)
    - [3. Conditional internal behavior](#3-Conditional-internal-behavior)
    - [4. Functional mapping of IERC3643 methods](#4-functional-mapping-of-IERC3643-methods)
    - [5. Internal common aggregation](#5-internal-common-aggregation)
    - [6. Unified role architecture](#6-unified-role-architecture)
    - [7. ERC20 primitive impacts in ERC3643 mode](#7-erc20-primitive-impacts-in-erc3643-mode)
- [Benefits](#benefits)
- [Implementation phases](#implementation-phases)

## Status

In progress

## Context

The ERC-3643 standard defines a set of interfaces to support regulatory-compliant token operations. These interfaces are published under the ERC-3643 GitHub repository [https://github.com/ERC-3643/ERCs/blob/master/assets/erc-3643/interfaces] and include components for identity, compliance, recovery, freezing, and more.

Among them, IERC3643 is the main interface that directly extends IERC20, and serves as the entry point for most of the standard's regulatory and operational features.

However, IERC3643 is defined as a monolithic interface, which is incompatible with our modular design principles based on the Diamond pattern (EIP-2535).

## Decision

### 1. Segregation of IERC3643 into functional modules

To align with our Diamond architecture, the monolithic IERC3643 interface will be split into independent functional modules, each implemented through:

- A dedicated interface (e.g., IERC3643Freeze, IERC3643Recovery, etc.)
- An internal contract that encapsulates the business logic and manages storage
- An external contract that exposes public methods and delegates to the internal logic
- A facet that connects the external contract to the Diamond via delegatecall

When functionality is shared across multiple modules—such as token metadata, pause state, or access control—logic is centralized in abstract internal contracts (e.g., ERC203643InternalCommon, PauseInternalCommon)

### 2. Reuse of existing modules

To ensure compliance with ERC-3643 while maximizing modularity and reuse, the following strategy has been adopted:

- **Unified Architecture**: Create unified modules that work for both ERC20 and ERC3643 standards
- **Automatic Behavior Adaptation**: Internal logic adapts based on ERC3643 initialization (presence of IdentityRegistry)
- **Role Unification**: Merge similar roles while maintaining granular permissions

### 3. Conditional internal behavior

The internal logic of the modules dynamically adapts based on whether the environment has been initialized as ERC-3643 compatible. This configuration affects both access control and operation execution, enabling additional validations and specific restrictions to be applied only when the context requires it.

### 4. Functional mapping of IERC3643 methods

| Method                                              | Module               |
| --------------------------------------------------- | -------------------- |
| `setName(string)`                                   | ERC3643 Metadata     |
| `setSymbol(string)`                                 | ERC3643 Metadata     |
| `setOnchainID(address)`                             | ERC3643 Metadata     |
| `version()`                                         | ERC3643 Metadata     |
| `onchainID()`                                       | ERC3643 Metadata     |
| `identityRegistry()`                                | ERC3643 Regulatory   |
| `compliance()`                                      | ERC3643 Regulatory   |
| `setIdentityRegistry(address)`                      | ERC3643 Regulatory   |
| `setCompliance(address)`                            | ERC3643 Regulatory   |
| `pause()`                                           | Pause                |
| `unpause()`                                         | Pause                |
| `paused()`                                          | Pause                |
| `setAddressFrozen(address,bool)`                    | ERC3643 Freeze       |
| `freezePartialTokens(address,uint256)`              | ERC3643 Freeze       |
| `unfreezePartialTokens(address,uint256)`            | ERC3643 Freeze       |
| `isFrozen(address)`                                 | ERC3643 Freeze       |
| `getFrozenTokens(address)`                          | ERC3643 Freeze       |
| `forceTransfer(address,address,uint256)`            | ERC203643 Controller |
| `mint(address,uint256)`                             | ERC203643 Capped     |
| `burn(address,uint256)`                             | ERC203643 Controller |
| `recoveryAddress(address,address,address)`          | ERC3643Recovery      |
| `batchTransfer(address[],uint256[])`                | ERC20                |
| `batchForceTransfer(address[],address[],uint256[])` | ERC203643 Controller |
| `batchMint(address[],uint256[])`                    | ERC203643 Capped     |
| `batchForceBurn(address[],uint256[])`               | ERC203643 Controller |
| `batchSetAddressFrozen(address[],bool[])`           | ERC3643 Freeze       |
| `batchFreezePartialTokens(address[],uint256[])`     | ERC3643 Freeze       |
| `batchUnfreezePartialTokens(address[],uint256[])`   | ERC3643 Freeze       |

_Note: `IERC3643` extends `IERC20`, so in addition to the methods listed above, all standard ERC-20 methods are also inherited and available._

---

#### ERC3643 Metadata Module

| Method                  | Module           | Interface        | Internal                | External        | Facet                | Returns   | Events                    | Comment                                                         |
| ----------------------- | ---------------- | ---------------- | ----------------------- | --------------- | -------------------- | --------- | ------------------------- | --------------------------------------------------------------- |
| `setName(string)`       | ERC3643 Metadata | IERC3643Metadata | ERC20Internal           | ERC3643Metadata | ERC3643MetadataFacet | `void`    | `UpdatedTokenInformation` | Updates ERC20 name + emits event. Only **METADATA_ROLE**        |
| `setSymbol(string)`     | ERC3643 Metadata | IERC3643Metadata | ERC20Internal           | ERC3643Metadata | ERC3643MetadataFacet | `void`    | `UpdatedTokenInformation` | Updates ERC20 symbol + emits event. Only **METADATA_ROLE**      |
| `setOnchainID(address)` | ERC3643 Metadata | IERC3643Metadata | ERC3643MetadataInternal | ERC3643Metadata | ERC3643MetadataFacet | `void`    | `UpdatedTokenInformation` | Updates onchainID storage + emits event. Only **METADATA_ROLE** |
| `version()`             | ERC3643 Metadata | IERC3643Metadata | ERC3643MetadataInternal | ERC3643Metadata | ERC3643MetadataFacet | `string`  | None                      | Returns current version string from storage                     |
| `onchainID()`           | ERC3643 Metadata | IERC3643Metadata | ERC3643MetadataInternal | ERC3643Metadata | ERC3643MetadataFacet | `address` | None                      | Returns current onchainID address from storage                  |

**Implementation Details:**

- **Logic**: ERC3643-specific metadata management with ERC20 state updates
- **Storage**: Updates both ERC20 variables (name, symbol) and ERC3643-specific storage (onchainID, version)
- **Access Control**: All setters require **`METADATA_ROLE`** (granular metadata management)
- **Events**: All setters emit `UpdatedTokenInformation` event for regulatory compliance

---

#### ERC3643 Regulatory Module

| Method                         | Module             | Interface          | Internal                  | External          | Facet                  | Returns             | Events                  | Comment                                                                             |
| ------------------------------ | ------------------ | ------------------ | ------------------------- | ----------------- | ---------------------- | ------------------- | ----------------------- | ----------------------------------------------------------------------------------- |
| `identityRegistry()`           | ERC3643 Regulatory | IERC3643Regulatory | ERC3643RegulatoryInternal | ERC3643Regulatory | ERC3643RegulatoryFacet | `IIdentityRegistry` | None                    | Returns current IdentityRegistry interface from storage                             |
| `compliance()`                 | ERC3643 Regulatory | IERC3643Regulatory | ERC3643RegulatoryInternal | ERC3643Regulatory | ERC3643RegulatoryFacet | `ICompliance`       | None                    | Returns current Compliance contract interface from storage                          |
| `setIdentityRegistry(address)` | ERC3643 Regulatory | IERC3643Regulatory | ERC3643RegulatoryInternal | ERC3643Regulatory | ERC3643RegulatoryFacet | `void`              | `IdentityRegistryAdded` | Updates IdentityRegistry reference + enables ERC3643 mode. Only **REGULATORY_ROLE** |
| `setCompliance(address)`       | ERC3643 Regulatory | IERC3643Regulatory | ERC3643RegulatoryInternal | ERC3643Regulatory | ERC3643RegulatoryFacet | `void`              | `ComplianceAdded`       | Updates Compliance reference + calls `bindToken`. Only **REGULATORY_ROLE**          |

**Implementation Details:**

- **Logic**: Manages references to external regulatory infrastructure contracts
- **Behavior Switch**: Setting IdentityRegistry enables ERC3643 mode globally via `ERC203643InternalCommon`
- **Integration**: `setCompliance` automatically calls `bindToken(address(this))` on the compliance contract
- **Access Control**: All setters require **`REGULATORY_ROLE`** (granular regulatory infrastructure management)
- **Events**: Each setter emits specific regulatory events

---

#### ERC3643 Freeze Module

| Method                                            | Module         | Interface      | Internal              | External      | Facet              | Returns   | Events           | Comment                                             |
| ------------------------------------------------- | -------------- | -------------- | --------------------- | ------------- | ------------------ | --------- | ---------------- | --------------------------------------------------- |
| `setAddressFrozen(address,bool)`                  | ERC3643 Freeze | IERC3643Freeze | ERC3643FreezeInternal | ERC3643Freeze | ERC3643FreezeFacet | `void`    | `AddressFrozen`  | Sets complete freeze status. Only **FREEZE_ROLE**   |
| `freezePartialTokens(address,uint256)`            | ERC3643 Freeze | IERC3643Freeze | ERC3643FreezeInternal | ERC3643Freeze | ERC3643FreezeFacet | `void`    | `TokensFrozen`   | Increases frozen token amount. Only **FREEZE_ROLE** |
| `unfreezePartialTokens(address,uint256)`          | ERC3643 Freeze | IERC3643Freeze | ERC3643FreezeInternal | ERC3643Freeze | ERC3643FreezeFacet | `void`    | `TokensUnfrozen` | Decreases frozen token amount. Only **FREEZE_ROLE** |
| `isFrozen(address)`                               | ERC3643 Freeze | IERC3643Freeze | ERC3643FreezeInternal | ERC3643Freeze | ERC3643FreezeFacet | `bool`    | None             | Returns if address is completely frozen             |
| `getFrozenTokens(address)`                        | ERC3643 Freeze | IERC3643Freeze | ERC3643FreezeInternal | ERC3643Freeze | ERC3643FreezeFacet | `uint256` | None             | Returns amount of partially frozen tokens           |
| `batchSetAddressFrozen(address[],bool[])`         | ERC3643 Freeze | IERC3643Freeze | ERC3643FreezeInternal | ERC3643Freeze | ERC3643FreezeFacet | `void`    | `AddressFrozen`  | Batch freeze/unfreeze multiple addresses            |
| `batchFreezePartialTokens(address[],uint256[])`   | ERC3643 Freeze | IERC3643Freeze | ERC3643FreezeInternal | ERC3643Freeze | ERC3643FreezeFacet | `void`    | `TokensFrozen`   | Batch freeze tokens for multiple addresses          |
| `batchUnfreezePartialTokens(address[],uint256[])` | ERC3643 Freeze | IERC3643Freeze | ERC3643FreezeInternal | ERC3643Freeze | ERC3643FreezeFacet | `void`    | `TokensUnfrozen` | Batch unfreeze tokens for multiple addresses        |

**Implementation Details:**

- **Logic**: ERC3643-specific freeze management with two-tier freezing (complete + partial), including batch operations for efficiency.
- **Storage**: Maintains separate freeze state independent of ERC20 balances.
- **Integration**: Consumed by `ERC203643InternalCommon._beforeTokenTransfer` for transfer restrictions.
- **Auto-Unfreeze**: Only controller operations can auto-unfreeze during force operations.
- **Access Control**: All setters and batch operations require **`FREEZE_ROLE`** (granular freeze management).
- **Events**: All freeze operations emit specific events for regulatory tracking, including batch events.
- **Batch Operations**: Batch functions allow freezing/unfreezing multiple accounts or token amounts in a single transaction, improving operational efficiency for large-scale compliance actions.

---

#### ERC203643 Controller Module

| Method                                              | Module               | Interface            | Internal      | External            | Facet                    | Returns | Events                                                           | Comment                                                                    |
| --------------------------------------------------- | -------------------- | -------------------- | ------------- | ------------------- | ------------------------ | ------- | ---------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `forceTransfer(address,address,uint256)`            | ERC203643 Controller | IERC203643Controller | ERC20Internal | ERC203643Controller | ERC203643ControllerFacet | `bool`  | `ForceTransfer` + `Transfer` + `TokensUnfrozen`\*                | **Unified**: ERC20 simple transfer / ERC3643 with identity + auto-unfreeze |
| `forceBurn(address,uint256)`                        | ERC203643 Controller | IERC203643Controller | ERC20Internal | ERC203643Controller | ERC203643ControllerFacet | `void`  | `ForceBurn` + `Transfer` + `TokensUnfrozen`\*                    | **Unified**: ERC20 simple burn / ERC3643 with auto-unfreeze                |
| `batchForceTransfer(address[],address[],uint256[])` | ERC203643 Controller | IERC203643Controller | ERC20Internal | ERC203643Controller | ERC203643ControllerFacet | `void`  | `ForceTransfer` + `Transfer` + `TokensUnfrozen`\* (per transfer) | **Batch**: Multiple force transfers in single transaction                  |
| `batchForceBurn(address[],uint256[])`               | ERC203643 Controller | IERC203643Controller | ERC20Internal | ERC203643Controller | ERC203643ControllerFacet | `void`  | `ForceBurn` + `Transfer` + `TokensUnfrozen`\* (per burn)         | **Batch**: Multiple force burns in single transaction                      |

**Implementation Details:**

- **Unified Logic**: Single implementation adapts behavior based on `_identityRegistry() != address(0)`
- **ERC20 Mode** (no IdentityRegistry):
    - Simple force operations via `ERC20Internal._transfer/_burn`
    - Events: `ForceTransfer`/`ForceBurn` + standard `Transfer` events only
- **ERC3643 Mode** (IdentityRegistry configured):
    - Identity verification in `ERC203643InternalCommon._beforeTokenTransfer`
    - Auto-unfreeze functionality for insufficient free balance
    - Events: `ForceTransfer`/`ForceBurn` + `Transfer` + conditional `TokensUnfrozen`
- **Batch Operations**:
    - Allow processing multiple force operations in a single transaction
    - Each operation in the batch emits individual events
    - **Gas Warning**: Large batches may exceed gas limits - use with caution
    - Array length validation ensures consistent parameter arrays
- **Access Control**: All operations require `CONTROLLER_ROLE`
- **Pausable**: All operations respect `whenNotPaused` modifier

\*`TokensUnfrozen` event only emitted in ERC3643 mode when auto-unfreezing occurs

---

#### ERC203643 Capped Module

| Method                           | Module           | Interface        | Internal                | External        | Facet                | Returns   | Events                | Comment                                                             |
| -------------------------------- | ---------------- | ---------------- | ----------------------- | --------------- | -------------------- | --------- | --------------------- | ------------------------------------------------------------------- |
| `mint(address,uint256)`          | ERC203643 Capped | IERC203643Capped | ERC20Internal           | ERC203643Capped | ERC203643CappedFacet | `void`    | `Transfer`            | **Unified**: ERC20 simple mint / ERC3643 with identity verification |
| `batchMint(address[],uint256[])` | ERC203643 Capped | IERC203643Capped | ERC20Internal           | ERC203643Capped | ERC203643CappedFacet | `void`    | `Transfer` (per mint) | **Batch**: Multiple mint operations in single transaction           |
| `setCap(uint256)`                | ERC203643 Capped | IERC203643Capped | ERC203643CappedInternal | ERC203643Capped | ERC203643CappedFacet | `void`    | `CapSet`              | Updates supply cap with validation. Only **CAP_ROLE**               |
| `cap()`                          | ERC203643 Capped | IERC203643Capped | ERC203643CappedInternal | ERC203643Capped | ERC203643CappedFacet | `uint256` | None                  | Returns current supply cap from storage                             |
| `initializeCap(uint256)`         | ERC203643 Capped | IERC203643Capped | ERC203643CappedInternal | ERC203643Capped | ERC203643CappedFacet | `void`    | `CapSet`              | One-time cap initialization. Uses initializer pattern               |

**Implementation Details:**

- **Unified Logic**: Single implementation adapts behavior based on `_identityRegistry() != address(0)`
- **ERC20 Mode** (no IdentityRegistry):
    - Simple minting via `ERC20Internal._mint` with cap validation
    - Events: Standard `Transfer` event (from address(0))
- **ERC3643 Mode** (IdentityRegistry configured):
    - Identity verification in `ERC203643InternalCommon._beforeTokenTransfer`
    - Same cap validation + regulatory compliance
    - Events: Standard `Transfer` event (from address(0))
- **Batch Operations**:
    - `batchMint` allows minting to multiple addresses in a single transaction
    - **Optimized cap validation**: Validates total batch amount against cap before any mints
    - **Gas Warning**: Large batches may exceed gas limits - use with caution
    - Array length validation ensures consistent parameter arrays
    - **Atomic execution**: All mints succeed or entire batch fails
- **Cap Management**:
    - `initializeCap`: One-time setup with initializer protection
    - `setCap`: Runtime cap updates with validation (new cap >= current supply)
    - Both emit `CapSet(operator, newCap)` event
- **Access Control**: `MINTER_ROLE` for mint operations, `CAP_ROLE` for cap management
- **Pausable**: Mint operations respect `whenNotPaused` modifier
- **Previous Interface**: Replaces `IERC20Capped` with unified functionality

---

#### ERC3643 Recovery Module

| Method                                     | Module           | Interface        | Internal                | External        | Facet                | Returns | Events            | Comment                                                                |
| ------------------------------------------ | ---------------- | ---------------- | ----------------------- | --------------- | -------------------- | ------- | ----------------- | ---------------------------------------------------------------------- |
| `recoveryAddress(address,address,address)` | ERC3643 Recovery | IERC3643Recovery | ERC203643InternalCommon | ERC3643Recovery | ERC3643RecoveryFacet | `bool`  | `RecoverySuccess` | Recovers tokens from lost wallet to new wallet. Only **RECOVERY_ROLE** |

**Implementation Details:**

- **Logic**: Allows authorized agents to recover tokens from a lost wallet to a new, verified wallet.
- **Identity Validation**: Requires cryptographic proof that the new wallet belongs to the investor (DID or OnchainID).
- **State Preservation**: Transfers all tokens, preserves frozen balances and address freeze status.
- **Identity Registry Integration**: Registers new wallet and deletes lost wallet in Identity Registry.
- **Access Control**: Only callable by accounts with **`RECOVERY_ROLE`**.
- **Events**: Emits `RecoverySuccess` on successful recovery.
- **Mode**: Only available in ERC3643 mode (requires Identity Registry).

---

#### ERC3643 Pause Module

| Method      | Module | Interface | Internal      | External | Facet      | Returns | Events     | Comment                                |
| ----------- | ------ | --------- | ------------- | -------- | ---------- | ------- | ---------- | -------------------------------------- |
| `pause()`   | Pause  | IPause    | PauseInternal | Pause    | PauseFacet | `void`  | `Paused`   | Pauses contract. Only **PAUSE_ROLE**   |
| `unpause()` | Pause  | IPause    | PauseInternal | Pause    | PauseFacet | `void`  | `Unpaused` | Unpauses contract. Only **PAUSE_ROLE** |
| `paused()`  | Pause  | IPause    | PauseInternal | Pause    | PauseFacet | `bool`  | None       | Returns pause state                    |

**Implementation Details:**

- **Logic**: Standard pausing mechanism for emergency or regulatory controls.
- **Access Control**: Only accounts with **`PAUSE_ROLE`** can pause/unpause.
- **Events**: Emits `Paused` and `Unpaused` events.
- **Diamond Integration**: Exposed via `PauseFacet` for modular architecture.

---

### 6. ERC20 primitive impacts in ERC3643 mode

#### Operation Behavior Matrix

The following table shows how each operation behaves in ERC20 mode vs ERC3643 mode:

##### **MINT Operations**

| Operation  | ERC20 Mode                      | ERC3643 Mode                                          |
| ---------- | ------------------------------- | ----------------------------------------------------- |
| **`mint`** | ✅ Sin validaciones adicionales | ✅ Receptor debe estar verificado en IdentityRegistry |

##### **BURN Operations**

| Operation                    | ERC20 Mode                         | ERC3643 Mode                             |
| ---------------------------- | ---------------------------------- | ---------------------------------------- |
| **`burn`** (normal)          | ✅ Solo validar balance suficiente | ❌ **NO EXISTE** (regulatory compliance) |
| **`burnFrom`** (normal)      | ✅ Solo validar balance suficiente | ❌ **NO EXISTE** (regulatory compliance) |
| **`forceBurn`** (controller) | ✅ Sin validaciones adicionales    | ✅ Auto-unfreeze si es necesario         |

##### **TRANSFER Operations**

| Operation                        | ERC20 Mode                      | ERC3643 Mode                                                            |
| -------------------------------- | ------------------------------- | ----------------------------------------------------------------------- |
| **`transfer`** (normal)          | ✅ Sin validaciones adicionales | ✅ Receptor verificado + ambos no congelados + balance libre suficiente |
| **`transferFrom`** (normal)      | ✅ Sin validaciones adicionales | ✅ Receptor verificado + ambos no congelados + balance libre suficiente |
| **`forceTransfer`** (controller) | ✅ Sin validaciones adicionales | ✅ Receptor verificado + auto-unfreeze si es necesario                  |

#### Implementation Details

**Transfer Operations** (`transfer`, `transferFrom` - via `ERC20Facet`):

- **ERC20 Mode**: Standard transfer logic
- **ERC3643 Mode**:
    - Recipient must be verified in Identity Registry
    - Sender must not be frozen (`isFrozen`)
    - Recipient must not be frozen (`isFrozen`)
    - Must have sufficient free (unfrozen) balance
    - **Note**: Normal transfers do NOT auto-unfreeze tokens (only force operations do)

**Minting Operations** (`mint` - via `ERC203643CappedFacet`):

- **ERC20 Mode**: Simple minting respecting cap
- **ERC3643 Mode**: Recipient must be verified in Identity Registry + cap validation

**Burning Operations**:

- **ERC20 Mode**: Standard user burn via `ERC20BurnableFacet`
- **ERC3643 Mode**:
    - **`ERC20BurnableFacet` not exposed** (regulatory compliance)
    - **Only `forceBurn`** available via `ERC203643ControllerFacet`
    - Auto-unfreeze capability only for controllers

**Unaffected Operations**: `approve`, `allowance`, `balanceOf`, `totalSupply` - remain standard in both modes via `ERC20Facet`.

**Facet Exposure Notes:**

- **ERC20 Mode**: All facets available (`ERC20Facet`, `ERC20BurnableFacet`, `ERC203643ControllerFacet`, etc.)
- **ERC3643 Mode**: `ERC20BurnableFacet` **not exposed** - only controllers can burn via `ERC203643ControllerFacet`

**Technical Note**: Enhanced behaviors are automatically applied in `ERC203643InternalCommon._beforeTokenTransfer` based on `_identityRegistry() != address(0)`.

### 7. Unified role architecture

**Context:**
The ERC3643 originally defines two main roles based on the interface comments:

- `TOKEN_OWNER_ROLE`: For "owner of the token smart contract" operations (metadata and regulatory setup)
- `TOKEN_AGENT_ROLE`: For "wallet set as agent of the token" operations (operational functions)

While ERC20 used specific roles like `CONTROLLER_ROLE` and `MINTER_ROLE`.

**Decision:**
Implement maximum granular role architecture maintaining ERC3643 semantics:

**Owner-level roles (infrastructure setup - granular breakdown of TOKEN_OWNER_ROLE):**

- **METADATA_ROLE**: For token metadata management
    - `setName`, `setSymbol`, `setOnchainID` (token branding and identity)
- **REGULATORY_ROLE**: For regulatory infrastructure management
    - `setIdentityRegistry`, `setCompliance` (compliance setup)

**Agent-level roles (operational functions - granular breakdown of TOKEN_AGENT_ROLE):**

- **CONTROLLER_ROLE**: For force operations
    - `forceTransfer`, `forceBurn` (replaces ERC20 controller functionality)
- **MINTER_ROLE**: For minting operations
    - `mint` (maintains existing ERC20Capped role)
- **FREEZE_ROLE**: For freeze/unfreeze operations
    - `setAddressFrozen`, `freezePartialTokens`, `unfreezePartialTokens`
- **PAUSE_ROLE**: For pause/unpause operations
    - `pause`, `unpause`
- **CAP_ROLE**: For cap management
    - `setCap`, `initializeCap`
- **RECOVERY_ROLE**: For recovery operations
    - `recoveryAddress` _(pending implementation)_

**For wallets needing full ERC3643 "owner" functionality:**

- Assign owner roles: `METADATA_ROLE` + `REGULATORY_ROLE`
- This provides complete equivalence with original `TOKEN_OWNER_ROLE` but with granular control

**For wallets needing full ERC3643 "agent" functionality:**

- Assign operational roles: `CONTROLLER_ROLE` + `MINTER_ROLE` + `FREEZE_ROLE` + `PAUSE_ROLE` + `RECOVERY_ROLE`
- This provides complete equivalence with original `TOKEN_AGENT_ROLE` but with granular control

**Benefits:**

- **Maximum permission granularity**: Each operation type has its own role
- **ERC3643 semantic compatibility**: Maintains owner vs agent distinction
- **Organizational flexibility**: Different teams can have different owner-level permissions
- **Principle of least privilege**: Assign only the permissions needed
- **Clear separation**: Infrastructure setup vs operational functions vs metadata management
- **Compatibility**: Maintains existing ERC20 architecture patterns

**Role Mapping Table:**

| Function                | Original ERC3643 Role | New Granular Role           | Level          | Comment                       |
| ----------------------- | --------------------- | --------------------------- | -------------- | ----------------------------- |
| `setName`               | Owner                 | `METADATA_ROLE`             | Infrastructure | Token branding/metadata       |
| `setSymbol`             | Owner                 | `METADATA_ROLE`             | Infrastructure | Token branding/metadata       |
| `setOnchainID`          | Owner                 | `METADATA_ROLE`             | Infrastructure | Token branding/metadata       |
| `setIdentityRegistry`   | Owner                 | `REGULATORY_ROLE`           | Infrastructure | Regulatory setup              |
| `setCompliance`         | Owner                 | `REGULATORY_ROLE`           | Infrastructure | Regulatory setup              |
| `pause`                 | Agent                 | `PAUSE_ROLE`                | Operational    | Emergency controls            |
| `unpause`               | Agent                 | `PAUSE_ROLE`                | Operational    | Emergency controls            |
| `setAddressFrozen`      | Agent                 | `FREEZE_ROLE`               | Operational    | Freeze management             |
| `freezePartialTokens`   | Agent                 | `FREEZE_ROLE`               | Operational    | Freeze management             |
| `unfreezePartialTokens` | Agent                 | `FREEZE_ROLE`               | Operational    | Freeze management             |
| `forcedTransfer`        | Agent                 | `CONTROLLER_ROLE`           | Operational    | Force operations              |
| `mint`                  | Agent                 | `MINTER_ROLE`               | Operational    | Token supply management       |
| `burn`                  | Agent                 | `CONTROLLER_ROLE`           | Operational    | Force operations              |
| `recoveryAddress`       | Agent                 | `RECOVERY_ROLE`             | Operational    | Recovery operations           |
| Batch operations        | Agent                 | Respective individual roles | Operational    | Same as individual operations |

## Benefits

- **Unified Architecture**: Single codebase handles both ERC20 and ERC3643 standards
- **Automatic Adaptation**: Behavior changes based on regulatory context without code duplication
- **Maximum Role Granularity**: Each operation type has its own specific role
- **Organizational Flexibility**: Different teams can have different permission levels (metadata vs regulatory vs operational)
- **Diamond Compatibility**: Aligns with modular architecture principles
- **Reduced Complexity**: Eliminates need for separate ERC20 and ERC3643 implementations
- **Improved Maintainability**: Single point of logic for common operations
- **Regulatory Compliance**: Automatic enforcement of ERC3643 rules when IdentityRegistry is configured
- **Backwards Compatibility**: ERC20 behavior preserved when regulatory components are not initialized

**Next Steps:**

- Update deployment scripts to use unified modules and granular roles
- Migrate existing tests to unified interfaces
- Document behavior differences between ERC20 and ERC3643 modes
- Consider reorganizing folder structure to reflect unified architecture

---
